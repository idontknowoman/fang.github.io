<?xml version='1.0' encoding='UTF-8'?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0"><channel><title>Blog Title</title><link>https://idontknowoman.github.io/fang.github.io</link><description>Blog description</description><copyright>Blog Title</copyright><docs>http://www.rssboard.org/rss-specification</docs><generator>python-feedgen</generator><image><url>https://github.githubassets.com/favicons/favicon.svg</url><title>avatar</title><link>https://idontknowoman.github.io/fang.github.io</link></image><lastBuildDate>Fri, 13 Dec 2024 05:48:33 +0000</lastBuildDate><managingEditor>Blog Title</managingEditor><ttl>60</ttl><webMaster>Blog Title</webMaster><item><title>C++实现通讯录管理系统</title><link>https://idontknowoman.github.io/fang.github.io/post/C%2B%2B-shi-xian-tong-xun-lu-guan-li-xi-tong.html</link><description>用C++实现一个通讯录管理系统，实现以下功能:

1. 创建通讯录(包括姓名,性别,年龄,联系电话,家庭住址)

2. 显示通讯录(显示通讯录成员的基本信息)

3. 删除通讯录(删除通讯录中想删除的联系人)

4. 查找通讯录(按照联系人的名字查找到具体的联系人)

5. 修改通讯录(修改通讯录中联系人的基本信息)

6. 清空通讯录(删除通讯录中的所有联系人)

7. 退出通讯录(退出通讯录界面)



#### 考察知识点：

- struct结构体——定义、构造、初始化
- 枚举类型
- switch-case
- 数组操作
- 链表操作

#### 功能实现：

- 查询界面展示
- 增删查改



#### struct结构体

- struct结构体的定义

  - 代码如下：

    - ```c++
      struct Person {
          string name;
          Sex sex; // male or female
          int age;
          string phoneNumber;
          string address;
      
          Person() : name(''), sex(FEMALE), age(0), phoneNumber(''), address('') {};
          Person(string n, Sex s, int a, string pN, string addr) : name(n), sex(s), age(a), phoneNumber(pN), address(addr) {};
      };
      ```

    - 包含属性声明、方法声明、构造方法声明

    - struct与C++中类的区别

      - struct声明的结构体中的变量和方法的访问符都是public

  - 与C的不同

    - C在声明结构体时必须带struct，而C++中可以省略struct，这也是C中的结构体定义常使用typedef取别名的原因

  - 起别名方式

    - typedef（沿用C）

      - ```c++
        typedef struct Person {
            string name;
            Sex sex; // male or female
            int age;
            string phoneNumber;
            string address;
        
            Person() : name(''), sex(FEMALE), age(0), phoneNumber(''), address('') {};
            Person(string n, Sex s, int a, string pN, string addr) : name(n), sex(s), age(a), phoneNumber(pN), address(addr) {};
        } per;
        ```

    - 使用using（C++11及以后加入的新特性）

      - ```c++
        using per = struct Person;
        ```

- struct结构体的构造方法

  - 无参构造：`Person() : name(''), sex(FEMALE), age(0), phoneNumber(''), address('') {};`
  - 有参构造：`Person(string n, Sex s, int a, string pN, string addr) : name(n), sex(s), age(a), phoneNumber(pN), address(addr) {};`

- struct结构体的初始化

  - 直接在结构体定义的时候声明变量

    - ```c++
      struct Person {
          string name;
          Sex sex; // male or female
          int age;
          string phoneNumber;
          string address;
      
          Person() : name(''), sex(FEMALE), age(0), phoneNumber(''), address('') {};
          Person(string n, Sex s, int a, string pN, string addr) : name(n), sex(s), age(a), phoneNumber(pN), address(addr) {};
      } per;
      ```

  - 其他位置声明变量&lt;!--(当Person结构体存在无参构造方法或Person结构体只包含基本类型时，可以直接用Person()初始化)--&gt;

    - 兼容C: `struct Person person = struct Person()`
    - C++特性: `Person person = Person()`



#### 查询界面展示

输出提示，获取用户输入的选项，用户输入作为循环跳出条件（`swicth-case`处理各种情况）

代码如下：

```c++
	while (true) {
        cout &lt;&lt; '********请选择功能: ********' &lt;&lt; endl;
        // 1. 创建通讯录(包括姓名,性别,年龄,联系电话,家庭住址)
        cout &lt;&lt; '******* 1.创建联系人 *******' &lt;&lt; endl;
        // 2. 显示通讯录(显示通讯录成员的基本信息)
        cout &lt;&lt; '******* 2.显示通讯录 *******' &lt;&lt; endl;
        // 3. 删除通讯录(删除通讯录中想删除的联系人)
        cout &lt;&lt; '******* 3.删除联系人 *******' &lt;&lt; endl;
        // 4. 查找通讯录(按照联系人的名字查找到具体的联系人)
        cout &lt;&lt; '******* 4.查找联系人 *******' &lt;&lt; endl;
        // 5. 修改通讯录(修改通讯录中联系人的基本信息)
        cout &lt;&lt; '******* 5.修改联系人 *******' &lt;&lt; endl;
        // 6. 清空通讯录(删除通讯录中的所有联系人)
        cout &lt;&lt; '******* 6.清空通讯录 *******' &lt;&lt; endl;
        // 0. 退出通讯录(退出通讯录界面)
        cout &lt;&lt; '******* 0.退出通讯录 *******' &lt;&lt; endl;
        int choice;
        cin &gt;&gt; choice;
        switch (choice) {
            case 0:
                cout &lt;&lt; '已退出通讯录系统' &lt;&lt; endl;
                return 0;
            case 1:
                // 增加联系人
                addContact(contact);
                break;
            case 2:
                showContact(contact);
                break;
            case 3:
                deleteContact(contact);
                break;
            case 4:
                searchContact(contact);
                break;
            case 5:
                modifyContact(contact);
                break;
            case 6:
                clearContact(contact);
                break;
            default:
                cout &lt;&lt; '选项不在功能内! 请重新选择' &lt;&lt; endl;
                break;
        }
    }
```



#### 增删查改

对通讯录的管理可概括为增删查改功能，可采用以下两种数据结构实现

- 数组：
  - 优点：查找效率高
  - 缺点：删除效率低（后续元素都受到影响），容量固定
- 链表：
  - 优点：删除效率高（改变指针链接即可），容量不限，更灵活
  - 缺点：查找效率低



##### 数组实现的代码（完整版）：

```c++
#include &lt;iostream&gt;
using namespace std;

enum Sex{
    MALE,
    FEMALE
};

struct Person {
    string name;
    Sex sex; // male or female
    int age;
    string phoneNumber;
    string address;

    Person() : name(''), sex(FEMALE), age(0), phoneNumber(''), address('') {};
    Person(string n, Sex s, int a, string pN, string addr) : name(n), sex(s), age(a), phoneNumber(pN), address(addr) {};
};

// 数组存储
struct Contact
{
    struct Person person[1000];
    int cnt; // 存储通讯录中当前联系人数量

    Contact() : cnt(0) {};
};


int addContact(Contact&amp; contact) {
    if (contact.cnt &gt;= 1000) {
        cout &lt;&lt; '通讯录已满，无法继续存储' &lt;&lt; endl;
        return 0;
    }
    string name;
    cout &lt;&lt; '请输入新增联系人姓名' &lt;&lt; endl;
    cin &gt;&gt; name;
    int sex;
    cout &lt;&lt; '请输入新增联系人性别(0表示男性, 1表示女性)' &lt;&lt; endl;
    cin &gt;&gt; sex;
    while (sex != 0 &amp;&amp; sex != 1) {
        cout &lt;&lt; '输入不符合要求，请重新输入！' &lt;&lt; endl;
        cin &gt;&gt; sex;
    }
    int age;
    cout &lt;&lt; '请输入新增联系人年龄' &lt;&lt; endl;
    cin &gt;&gt; age;
    string phoneNumber;
    cout &lt;&lt; '请输入新增联系人联系电话' &lt;&lt; endl;
    cin &gt;&gt; phoneNumber;
    string address;
    cout &lt;&lt; '请输入新增联系人家庭住址' &lt;&lt; endl;
    cin &gt;&gt; address;
    Person person(name, static_cast&lt;Sex&gt;(sex), age, phoneNumber, address);
    contact.person[contact.cnt] = person;
    contact.cnt++;
    return 1;
}

int showContact(Contact&amp; contact) {
    cout &lt;&lt; '通讯录列表如下:' &lt;&lt; endl;
    for (int i = 0; i &lt; contact.cnt; i++) {
        cout &lt;&lt; '********************************' &lt;&lt; endl;
        cout &lt;&lt; '第' &lt;&lt; i + 1 &lt;&lt; '个联系人:' &lt;&lt; endl;
        cout &lt;&lt; '姓名' &lt;&lt; contact.person[i].name &lt;&lt; endl;
        cout &lt;&lt; '性别' &lt;&lt; contact.person[i].sex &lt;&lt; endl;
        cout &lt;&lt; '年龄' &lt;&lt; contact.person[i].age &lt;&lt; endl;
        cout &lt;&lt; '联系电话' &lt;&lt; contact.person[i].phoneNumber &lt;&lt; endl;
        cout &lt;&lt; '家庭住址' &lt;&lt; contact.person[i].address &lt;&lt; endl;
    }
    return 1;
}

int deleteContact(Contact&amp; contact) {
    cout &lt;&lt; '请输入你想删除的联系人姓名: ' &lt;&lt; endl;
    string name;
    cin &gt;&gt; name;
    for (int i = 0; i &lt; contact.cnt; i++) {
        if (contact.person[i].name == name) {
            for (int j = i + 1; j &lt; contact.cnt; j++) {
                contact.person[j-1] = contact.person[j];
            }
            contact.cnt--;
            return 1;
        }
    }
    cout &lt;&lt; '未查找到该联系人' &lt;&lt; endl;
    return 0;
}

int searchContact(Contact &amp;contact) {
    cout &lt;&lt; '请输入你想查找的联系人姓名: ' &lt;&lt; endl;
    string name;
    cin &gt;&gt; name;
    for (const Person&amp; person : contact.person) {
        if (person.name == name) {
            cout &lt;&lt; '姓名' &lt;&lt; person.name &lt;&lt; endl;
            cout &lt;&lt; '性别' &lt;&lt; person.sex &lt;&lt; endl;
            cout &lt;&lt; '年龄' &lt;&lt; person.age &lt;&lt; endl;
            cout &lt;&lt; '联系电话' &lt;&lt; person.phoneNumber &lt;&lt; endl;
            cout &lt;&lt; '家庭住址' &lt;&lt; person.address &lt;&lt; endl;
            return 1;
        }
    }
    cout &lt;&lt; '未查找到该联系人' &lt;&lt; endl;
    return 0;
}

int modifyContact(Contact&amp; contact) {
    cout &lt;&lt; '请输入你想修改的联系人姓名: ' &lt;&lt; endl;
    string name;
    cin &gt;&gt; name;
    for (Person &amp; person : contact.person) {
        if (person.name == name) {
            int sex, age;
            string phoneNumber, address;
            cout &lt;&lt; '请输入修改后的联系人性别(0表示男性, 1表示女性)' &lt;&lt; endl;
            cin &gt;&gt; sex;
            while (sex != 0 &amp;&amp; sex != 1) {
                cout &lt;&lt; '输入不符合要求，请重新输入！' &lt;&lt; endl;
                cin &gt;&gt; sex;
            }
            cout &lt;&lt; '请输入修改后的联系人年龄' &lt;&lt; endl;
            cin &gt;&gt; age;
            cout &lt;&lt; '请输入修改后的联系人联系电话' &lt;&lt; endl;
            cin &gt;&gt; phoneNumber;
            cout &lt;&lt; '请输入修改后的联系人家庭住址' &lt;&lt; endl;
            cin &gt;&gt; address;
            person.sex = static_cast&lt;Sex&gt;(sex);
            person.age = age;
            person.phoneNumber = phoneNumber;
            person.address = address;
            cout &lt;&lt; '修改成功' &lt;&lt; endl;
            return 1;
        }
    }
    cout &lt;&lt; '未查找到该联系人' &lt;&lt; endl;
    return 0;
}

int clearContact(Contact&amp; contact) {
    contact.cnt = 0;
    cout &lt;&lt; '通讯录已清空' &lt;&lt; endl;
    return 1;
}

int main()
{
    cout &lt;&lt; '****欢迎来到通讯录管理系统****' &lt;&lt; endl;
    Contact contact;
    contact.cnt = 0;
    while (true) {
        cout &lt;&lt; '********请选择功能: ********' &lt;&lt; endl;
        // 1. 创建通讯录(包括姓名,性别,年龄,联系电话,家庭住址)
        cout &lt;&lt; '******* 1.创建联系人 *******' &lt;&lt; endl;
        // 2. 显示通讯录(显示通讯录成员的基本信息)
        cout &lt;&lt; '******* 2.显示通讯录 *******' &lt;&lt; endl;
        // 3. 删除通讯录(删除通讯录中想删除的联系人)
        cout &lt;&lt; '******* 3.删除联系人 *******' &lt;&lt; endl;
        // 4. 查找通讯录(按照联系人的名字查找到具体的联系人)
        cout &lt;&lt; '******* 4.查找联系人 *******' &lt;&lt; endl;
        // 5. 修改通讯录(修改通讯录中联系人的基本信息)
        cout &lt;&lt; '******* 5.修改联系人 *******' &lt;&lt; endl;
        // 6. 清空通讯录(删除通讯录中的所有联系人)
        cout &lt;&lt; '******* 6.清空通讯录 *******' &lt;&lt; endl;
        // 0. 退出通讯录(退出通讯录界面)
        cout &lt;&lt; '******* 0.退出通讯录 *******' &lt;&lt; endl;
        int choice;
        cin &gt;&gt; choice;
        switch (choice) {
            case 0:
                cout &lt;&lt; '已退出通讯录系统' &lt;&lt; endl;
                return 0;
            case 1:
                // 增加联系人
                addContact(contact);
                break;
            case 2:
                showContact(contact);
                break;
            case 3:
                deleteContact(contact);
                break;
            case 4:
                searchContact(contact);
                break;
            case 5:
                modifyContact(contact);
                break;
            case 6:
                clearContact(contact);
                break;
            default:
                cout &lt;&lt; '选项不在功能内! 请重新选择' &lt;&lt; endl;
                break;
        }
    }
}

```



##### 链表实现的代码（完整版）：

```c++
#include &lt;iostream&gt;
using namespace std;

enum Sex{
    MALE,
    FEMALE
};

struct Person {
    string name;
    Sex sex; // male or female
    int age;
    string phoneNumber;
    string address;

    Person() : name(''), sex(FEMALE), age(0), phoneNumber(''), address('') {};
    Person(string n, Sex s, int a, string pN, string addr) : name(n), sex(s), age(a), phoneNumber(pN), address(addr) {};
};

// 链表存储
struct Contact
{
    Person person;
    Contact* next;

    Contact() : person(), next(nullptr) {};
};


int addContact(Contact&amp; contact) {
    string name;
    cout &lt;&lt; '请输入新增联系人姓名' &lt;&lt; endl;
    cin &gt;&gt; name;
    int sex;
    cout &lt;&lt; '请输入新增联系人性别(0表示男性, 1表示女性)' &lt;&lt; endl;
    cin &gt;&gt; sex;
    while (sex != 0 &amp;&amp; sex != 1) {
        cout &lt;&lt; '输入不符合要求，请重新输入！' &lt;&lt; endl;
        cin &gt;&gt; sex;
    }
    int age;
    cout &lt;&lt; '请输入新增联系人年龄' &lt;&lt; endl;
    cin &gt;&gt; age;
    string phoneNumber;
    cout &lt;&lt; '请输入新增联系人联系电话' &lt;&lt; endl;
    cin &gt;&gt; phoneNumber;
    string address;
    cout &lt;&lt; '请输入新增联系人家庭住址' &lt;&lt; endl;
    cin &gt;&gt; address;
    Person person(name, static_cast&lt;Sex&gt;(sex), age, phoneNumber, address);
    Contact* newContact = new Contact;
    newContact-&gt;person = person;
    newContact-&gt;next = nullptr;
    Contact* p = &amp;contact;
    while (p-&gt;next != nullptr) {
        p = p-&gt;next;
    }
    p-&gt;next = newContact;
    return 1;
}

int showContact(Contact&amp; contact) {
    cout &lt;&lt; '通讯录列表如下:' &lt;&lt; endl;
    Contact* p = &amp;contact;
    p = p-&gt;next;
    int i = 0;
    while(p!= nullptr) {
        cout &lt;&lt; '********************************' &lt;&lt; endl;
        cout &lt;&lt; '第' &lt;&lt; i + 1 &lt;&lt; '个联系人:' &lt;&lt; endl;
        cout &lt;&lt; '姓名' &lt;&lt; p-&gt;person.name &lt;&lt; endl;
        cout &lt;&lt; '性别' &lt;&lt; p-&gt;person.sex &lt;&lt; endl;
        cout &lt;&lt; '年龄' &lt;&lt; p-&gt;person.age &lt;&lt; endl;
        cout &lt;&lt; '联系电话' &lt;&lt; p-&gt;person.phoneNumber &lt;&lt; endl;
        cout &lt;&lt; '家庭住址' &lt;&lt; p-&gt;person.address &lt;&lt; endl;
        p = p-&gt;next;
        i++;
    }
    return 1;
}

int deleteContact(Contact&amp; contact) {
    cout &lt;&lt; '请输入你想删除的联系人姓名: ' &lt;&lt; endl;
    string name;
    cin &gt;&gt; name;
    Contact* p = &amp;contact;
    while (p-&gt;next != nullptr) {
        if (p-&gt;next-&gt;person.name == name) {
            Contact* temp = p-&gt;next;
            p-&gt;next = temp-&gt;next;
            delete temp;
            cout &lt;&lt; '删除成功' &lt;&lt; endl;
            return 1;
        }
        p = p-&gt;next;
    }
    cout &lt;&lt; '未查找到该联系人' &lt;&lt; endl;
    return 0;
}

int searchContact(Contact &amp;contact) {
    cout &lt;&lt; '请输入你想查找的联系人姓名: ' &lt;&lt; endl;
    string name;
    cin &gt;&gt; name;
    Contact* p = &amp;contact;
    p = p-&gt;next;
    while (p != nullptr) {
        if (p-&gt;person.name == name) {
            cout &lt;&lt; '姓名' &lt;&lt; p-&gt;person.name &lt;&lt; endl;
            cout &lt;&lt; '性别' &lt;&lt; p-&gt;person.sex &lt;&lt; endl;
            cout &lt;&lt; '年龄' &lt;&lt; p-&gt;person.age &lt;&lt; endl;
            cout &lt;&lt; '联系电话' &lt;&lt; p-&gt;person.phoneNumber &lt;&lt; endl;
            cout &lt;&lt; '家庭住址' &lt;&lt; p-&gt;person.address &lt;&lt; endl;
            return 1;
        }
        p = p-&gt;next;
    }
    cout &lt;&lt; '未查找到该联系人' &lt;&lt; endl;
    return 0;
}

int modifyContact(Contact&amp; contact) {
    cout &lt;&lt; '请输入你想修改的联系人姓名: ' &lt;&lt; endl;
    string name;
    cin &gt;&gt; name;
    Contact* p = &amp;contact;
    p = p-&gt;next;
    while (p != nullptr) {
        if (p-&gt;person.name == name) {
            int sex, age;
            string phoneNumber, address;
            cout &lt;&lt; '请输入修改后的联系人性别(0表示男性, 1表示女性)' &lt;&lt; endl;
            cin &gt;&gt; sex;
            while (sex != 0 &amp;&amp; sex != 1) {
                cout &lt;&lt; '输入不符合要求，请重新输入！' &lt;&lt; endl;
                cin &gt;&gt; sex;
            }
            cout &lt;&lt; '请输入修改后的联系人年龄' &lt;&lt; endl;
            cin &gt;&gt; age;
            cout &lt;&lt; '请输入修改后的联系人联系电话' &lt;&lt; endl;
            cin &gt;&gt; phoneNumber;
            cout &lt;&lt; '请输入修改后的联系人家庭住址' &lt;&lt; endl;
            cin &gt;&gt; address;
            p-&gt;person.sex = static_cast&lt;Sex&gt;(sex);
            p-&gt;person.age = age;
            p-&gt;person.phoneNumber = phoneNumber;
            p-&gt;person.address = address;
            cout &lt;&lt; '修改成功' &lt;&lt; endl;
            return 1;
        }
        p = p-&gt;next;
    }
    cout &lt;&lt; '未查找到该联系人' &lt;&lt; endl;
    return 0;
}

int clearContact(Contact&amp; contact) {
    contact.next = nullptr;
    cout &lt;&lt; '通讯录已清空' &lt;&lt; endl;
    return 1;
}

int main()
{
    cout &lt;&lt; '****欢迎来到通讯录管理系统****' &lt;&lt; endl;
    Contact contact;
    while (true) {
        cout &lt;&lt; '********请选择功能: ********' &lt;&lt; endl;
        // 1. 创建通讯录(包括姓名,性别,年龄,联系电话,家庭住址)
        cout &lt;&lt; '******* 1.创建联系人 *******' &lt;&lt; endl;
        // 2. 显示通讯录(显示通讯录成员的基本信息)
        cout &lt;&lt; '******* 2.显示通讯录 *******' &lt;&lt; endl;
        // 3. 删除通讯录(删除通讯录中想删除的联系人)
        cout &lt;&lt; '******* 3.删除联系人 *******' &lt;&lt; endl;
        // 4. 查找通讯录(按照联系人的名字查找到具体的联系人)
        cout &lt;&lt; '******* 4.查找联系人 *******' &lt;&lt; endl;
        // 5. 修改通讯录(修改通讯录中联系人的基本信息)
        cout &lt;&lt; '******* 5.修改联系人 *******' &lt;&lt; endl;
        // 6. 清空通讯录(删除通讯录中的所有联系人)
        cout &lt;&lt; '******* 6.清空通讯录 *******' &lt;&lt; endl;
        // 0. 退出通讯录(退出通讯录界面)
        cout &lt;&lt; '******* 0.退出通讯录 *******' &lt;&lt; endl;
        int choice;
        cin &gt;&gt; choice;
        switch (choice) {
            case 0:
                cout &lt;&lt; '已退出通讯录系统' &lt;&lt; endl;
                return 0;
            case 1:
                // 增加联系人
                addContact(contact);
                break;
            case 2:
                showContact(contact);
                break;
            case 3:
                deleteContact(contact);
                break;
            case 4:
                searchContact(contact);
                break;
            case 5:
                modifyContact(contact);
                break;
            case 6:
                clearContact(contact);
                break;
            default:
                cout &lt;&lt; '选项不在功能内! 请重新选择' &lt;&lt; endl;
                break;
        }
    }
}
```。</description><guid isPermaLink="true">https://idontknowoman.github.io/fang.github.io/post/C%2B%2B-shi-xian-tong-xun-lu-guan-li-xi-tong.html</guid><pubDate>Fri, 13 Dec 2024 05:48:09 +0000</pubDate></item><item><title>C++实现一个简易计算器</title><link>https://idontknowoman.github.io/fang.github.io/post/C%2B%2B-shi-xian-yi-ge-jian-yi-ji-suan-qi.html</link><description>用C++实现一个简易计算器，实现小数的加减乘除计算的基本功能&#13;
#### 考察知识点：&#13;
- 输入输出&#13;
- 栈&#13;
- 异常处理&#13;
- 类型转换&#13;
- 函数传参&#13;
&#13;
#### 功能实现：&#13;
- 中缀表达式转后缀表达式&#13;
- 后缀表达式计算&#13;
&#13;
#### 中缀表达式转后缀表达式&#13;
首先获取输入的中缀表达式字符串，实现以下到后缀表达式的转换，如：&#13;
`3 + 5 * 10 ——&gt; 3 5 10 * +`&#13;
那么这里需要准备一个栈，控制操作符的进出顺序，操作数的顺序没有改变，直接保留即可&#13;
流程：&#13;
1. 定义一个指针，遍历中缀表达式字符串，遇到操作数，直接添加到结果&#13;
2. 遇到操作符，则判断情况，优先级大的操作符直接进栈&#13;
    1. 操作符为'(' or '*' or '\'：直接进栈（`stack.push(op)`）&#13;
    2. 操作符为'+' or '-': 一直出栈（`stack.pop()`），加入结果中的后缀表达式，直到栈顶（`stack.top()`）元素的操作符优先级不高于当前op&#13;
    3. 操作符为')'：一直出栈，，加入结果中的后缀表达式，直到栈顶元素为'('，出栈，不加入结果&#13;
3. 遍历结束，弹出所有栈中元素（循环终止条件`stack.empty()`）&#13;
&#13;
#### 后缀表达式的计算&#13;
维护一个操作数栈，&#13;
流程：&#13;
1. 定义一个指针，从左至右依次读取后缀表达式的元素，遇到操作数就进栈&#13;
2. 遇到操作符就计算（`switch-case`加减乘除四种情况），将栈顶两元素的值计算后重新压栈&#13;
&#13;
&#13;
#### 异常处理&#13;
**除0异常**:自定义异常`throw std::invalid_argument('Division by zero')`，需包含`&lt;stdexcept&gt;`头文件&#13;
&#13;
#### 类型转换&#13;
- `char`转`int`: `int(ch-'0')`即可&#13;
- `int`转`double`: 直接强制类型转换&#13;
&#13;
#### 函数传参&#13;
- 传值: 函数内部对参数的修改不会影响外部&#13;
- 传址: 函数参数中携带&amp;标识，标识传值，函数对该参数的修改是全局的&#13;
&#13;
#### 代码如下&#13;
```&#13;
#include&lt;iostream&gt;&#13;
#include &lt;stack&gt;&#13;
#include &lt;cmath&gt;&#13;
#include &lt;iomanip&gt;&#13;
#include &lt;sstream&gt;&#13;
using namespace std;&#13;
&#13;
std::string double_to_string(double value) {&#13;
    // 使用ostringstream创建字符串流&#13;
    ostringstream out;&#13;
    // 设置fixed格式以避免科学计数法&#13;
    out &lt;&lt; fixed &lt;&lt; setprecision(10) &lt;&lt; value; // 设置足够多的小数位，之后再处理&#13;
    std::string result = out.str();&#13;
    // 查找最后一个小数点的位置&#13;
    size_t pos = result.find('.');&#13;
    if (pos != string::npos) {&#13;
        // 去除小数点后无效的0&#13;
        result.erase(result.find_last_not_of('0') + 1, string::npos);&#13;
        // 如果小数点是最后一个字符，去掉小数点&#13;
        if (result.back() == '.') {&#13;
            result.pop_back();&#13;
        }&#13;
    }&#13;
    return result;&#13;
}&#13;
&#13;
double getnum(string&amp; input, int&amp; i) {&#13;
    int num = int(input[i] - '0');&#13;
    i++;&#13;
    while (i &lt; input.length() &amp;&amp; isdigit(input[i])) {&#13;
        num = num * 10 + int(input[i] - '0');&#13;
        i++;&#13;
    }&#13;
    double xiaoshu = 0;&#13;
    int index = 1;&#13;
    if (input[i] == '.') {&#13;
        i++;&#13;
        while (i &lt; input.length() &amp;&amp; isdigit(input[i])) {&#13;
            xiaoshu += (input[i] - '0') * pow(0.1, index);&#13;
            i++;&#13;
        }&#13;
    }&#13;
    double operand = num + xiaoshu;&#13;
    return operand;&#13;
}&#13;
&#13;
// 中缀表达式转后缀表达式&#13;
string infix_to_postfix(string&amp; input) {&#13;
    string output = '';&#13;
    stack&lt;char&gt; operators;&#13;
    // 3 + 5 * 10 ——&gt; 3 5 10 * +&#13;
    for (int i = 0; i &lt; input.length(); i++) {&#13;
        char c = input[i];&#13;
        if (isdigit(c)) {&#13;
            double operand = getnum(input, i);&#13;
            output += double_to_string(operand) + ' ';&#13;
            c = input[i];&#13;
        }&#13;
        if (c == '(' || c == '*' || c == '/') {&#13;
            operators.push(c);&#13;
        } else if (c == ')') {&#13;
            while (!operators.empty() &amp;&amp; operators.top() != '(') {&#13;
                output += string(1, operators.top()) + ' ';&#13;
                operators.pop();&#13;
            }&#13;
            if (!operators.empty() &amp;&amp; operators.top() == '(') {&#13;
                operators.pop();&#13;
            }&#13;
        } else if (c == '+' || c == '-') {&#13;
            while (!operators.empty() &amp;&amp; operators.top() != '(' &amp;&amp; (operators.top() == '*' || operators.top() == '/')) {&#13;
                output += string(1, operators.top()) + ' ';&#13;
                operators.pop();&#13;
            }&#13;
            operators.push(c);&#13;
        }&#13;
    }&#13;
    while (!operators.empty()) {&#13;
        output += string(1, operators.top()) + ' ';&#13;
        operators.pop();&#13;
    }&#13;
    return output;&#13;
}&#13;
&#13;
double calc(string&amp; postfix_expression) {&#13;
    stack&lt;double&gt; operands;&#13;
    for (int i = 0; i &lt; postfix_expression.length(); i++) {&#13;
        char c = postfix_expression[i];&#13;
        if (isdigit(c)) {&#13;
            double operand = getnum(postfix_expression, i);&#13;
            operands.push(operand);&#13;
            c = postfix_expression[i];&#13;
        }&#13;
        double op1, op2;&#13;
        if (c == '+' || c == '-' || c == '*' || c == '/') {&#13;
            op1 = operands.top();&#13;
            operands.pop();&#13;
            op2 = operands.top();&#13;
            operands.pop();&#13;
        } else {&#13;
            continue;&#13;
        }&#13;
        switch (c) {&#13;
            case '+':&#13;
                operands.push(op1 + op2);&#13;
                break;&#13;
            case '-':&#13;
                operands.push(op1 - op2);&#13;
                break;&#13;
            case '*':&#13;
                operands.push(op1 * op2);&#13;
                break;&#13;
            case '/': {&#13;
                if (op2 == 0) {&#13;
                    cout &lt;&lt; 'Error: division by zero' &lt;&lt; endl;&#13;
                    return 0;&#13;
                }&#13;
                operands.push(op1 / op2);&#13;
                break;&#13;
            }&#13;
        }&#13;
    }&#13;
    return operands.top();&#13;
}&#13;
&#13;
// 实现一个计算器&#13;
int main() {&#13;
    string input;&#13;
    while (true) {&#13;
        cout &lt;&lt; 'Enter an expression: ' &lt;&lt; endl;&#13;
        getline(cin, input);&#13;
        if (input == 'exit') {&#13;
            break;&#13;
        }&#13;
        string new_expression = infix_to_postfix(input);&#13;
        // 打印一下中间结果：后缀表达式&#13;
        cout &lt;&lt; 'Postfix expression: ' &lt;&lt; new_expression &lt;&lt; endl;&#13;
        // 根据后缀表达式计算结果&#13;
        double result = calc(new_expression);&#13;
        cout &lt;&lt; 'Result: ' &lt;&lt; result &lt;&lt; endl;&#13;
    }&#13;
}&#13;
```。</description><guid isPermaLink="true">https://idontknowoman.github.io/fang.github.io/post/C%2B%2B-shi-xian-yi-ge-jian-yi-ji-suan-qi.html</guid><pubDate>Wed, 04 Dec 2024 11:04:17 +0000</pubDate></item></channel></rss>